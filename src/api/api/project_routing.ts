/*
 * Copyright Elasticsearch B.V. and contributors
 * SPDX-License-Identifier: Apache-2.0
 */

/* eslint-disable import/export */
/* eslint-disable @typescript-eslint/no-misused-new */
/* eslint-disable @typescript-eslint/no-extraneous-class */
/* eslint-disable @typescript-eslint/no-unused-vars */

// This file was automatically generated by elastic/elastic-client-generator-js
// DO NOT MODIFY IT BY HAND. Instead, modify the source open api file,
// and elastic/elastic-client-generator-js to regenerate this file again.

import {
  Transport,
  TransportRequestMetadata,
  TransportRequestOptions,
  TransportRequestOptionsWithMeta,
  TransportRequestOptionsWithOutMeta,
  TransportResult
} from '@elastic/transport'
import * as T from '../types'
import { kAcceptedParams } from '../../client'

interface That {
  transport: Transport
  [kAcceptedParams]: Record<string, { path: string[], body: string[], query: string[] }>
}

const commonQueryParams = ['error_trace', 'filter_path', 'human', 'pretty']

export default class ProjectRouting {
  transport: Transport
  [kAcceptedParams]: Record<string, { path: string[], body: string[], query: string[] }>
  constructor (transport: Transport) {
    this.transport = transport
    this[kAcceptedParams] = {
      'project_routing.create': {
        path: [
          'name'
        ],
        body: [
          'expressions'
        ],
        query: []
      },
      'project_routing.create_many': {
        path: [],
        body: [
          'expressions'
        ],
        query: []
      },
      'project_routing.delete': {
        path: [
          'name'
        ],
        body: [],
        query: []
      },
      'project_routing.get': {
        path: [
          'name'
        ],
        body: [],
        query: []
      },
      'project_routing.get_many': {
        path: [],
        body: [],
        query: []
      }
    }
  }

  /**
    * Create of update a single named project routing expression. Create of update a single named project routing expression.
    */
  async create (this: That, params: T.ProjectRoutingCreateRequest, options?: TransportRequestOptionsWithOutMeta): Promise<T.ProjectRoutingCreateResponse>
  async create (this: That, params: T.ProjectRoutingCreateRequest, options?: TransportRequestOptionsWithMeta): Promise<TransportResult<T.ProjectRoutingCreateResponse, unknown>>
  async create (this: That, params: T.ProjectRoutingCreateRequest, options?: TransportRequestOptions): Promise<T.ProjectRoutingCreateResponse>
  async create (this: That, params: T.ProjectRoutingCreateRequest, options?: TransportRequestOptions): Promise<any> {
    const {
      path: acceptedPath,
      body: acceptedBody,
      query: acceptedQuery
    } = this[kAcceptedParams]['project_routing.create']

    const userQuery = params?.querystring
    const querystring: Record<string, any> = userQuery != null ? { ...userQuery } : {}

    let body: any = params.body ?? undefined
    for (const key in params) {
      if (acceptedBody.includes(key)) {
        // @ts-expect-error
        body = params[key]
      } else if (acceptedPath.includes(key)) {
        continue
      } else if (key !== 'body' && key !== 'querystring') {
        if (acceptedQuery.includes(key) || commonQueryParams.includes(key)) {
          // @ts-expect-error
          querystring[key] = params[key]
        } else {
          body = body ?? {}
          // @ts-expect-error
          body[key] = params[key]
        }
      }
    }

    const method = 'PUT'
    const path = `/_project_routing/${encodeURIComponent(params.name.toString())}`
    const meta: TransportRequestMetadata = {
      name: 'project_routing.create',
      pathParts: {
        name: params.name
      },
      acceptedParams: [
        'name',
        'expressions'
      ]
    }
    return await this.transport.request({ path, method, querystring, body, meta }, options)
  }

  /**
    * Create of update named project routing expressions. Create or update named project routing expressions.
    */
  async createMany (this: That, params: T.ProjectRoutingCreateManyRequest, options?: TransportRequestOptionsWithOutMeta): Promise<T.ProjectRoutingCreateManyResponse>
  async createMany (this: That, params: T.ProjectRoutingCreateManyRequest, options?: TransportRequestOptionsWithMeta): Promise<TransportResult<T.ProjectRoutingCreateManyResponse, unknown>>
  async createMany (this: That, params: T.ProjectRoutingCreateManyRequest, options?: TransportRequestOptions): Promise<T.ProjectRoutingCreateManyResponse>
  async createMany (this: That, params: T.ProjectRoutingCreateManyRequest, options?: TransportRequestOptions): Promise<any> {
    const {
      path: acceptedPath,
      body: acceptedBody,
      query: acceptedQuery
    } = this[kAcceptedParams]['project_routing.create_many']

    const userQuery = params?.querystring
    const querystring: Record<string, any> = userQuery != null ? { ...userQuery } : {}

    let body: any = params.body ?? undefined
    for (const key in params) {
      if (acceptedBody.includes(key)) {
        // @ts-expect-error
        body = params[key]
      } else if (acceptedPath.includes(key)) {
        continue
      } else if (key !== 'body' && key !== 'querystring') {
        if (acceptedQuery.includes(key) || commonQueryParams.includes(key)) {
          // @ts-expect-error
          querystring[key] = params[key]
        } else {
          body = body ?? {}
          // @ts-expect-error
          body[key] = params[key]
        }
      }
    }

    const method = 'PUT'
    const path = '/_project_routing'
    const meta: TransportRequestMetadata = {
      name: 'project_routing.create_many',
      acceptedParams: [
        'expressions'
      ]
    }
    return await this.transport.request({ path, method, querystring, body, meta }, options)
  }

  /**
    * Delete named project routing expressions. Delete named project routing expressions.
    */
  async delete (this: That, params: T.ProjectRoutingDeleteRequest, options?: TransportRequestOptionsWithOutMeta): Promise<T.ProjectRoutingDeleteResponse>
  async delete (this: That, params: T.ProjectRoutingDeleteRequest, options?: TransportRequestOptionsWithMeta): Promise<TransportResult<T.ProjectRoutingDeleteResponse, unknown>>
  async delete (this: That, params: T.ProjectRoutingDeleteRequest, options?: TransportRequestOptions): Promise<T.ProjectRoutingDeleteResponse>
  async delete (this: That, params: T.ProjectRoutingDeleteRequest, options?: TransportRequestOptions): Promise<any> {
    const {
      path: acceptedPath
    } = this[kAcceptedParams]['project_routing.delete']

    const userQuery = params?.querystring
    const querystring: Record<string, any> = userQuery != null ? { ...userQuery } : {}

    let body: Record<string, any> | string | undefined
    const userBody = params?.body
    if (userBody != null) {
      if (typeof userBody === 'string') {
        body = userBody
      } else {
        body = { ...userBody }
      }
    }

    for (const key in params) {
      if (acceptedPath.includes(key)) {
        continue
      } else if (key !== 'body' && key !== 'querystring') {
        // @ts-expect-error
        querystring[key] = params[key]
      }
    }

    const method = 'DELETE'
    const path = `/_project_routing/${encodeURIComponent(params.name.toString())}`
    const meta: TransportRequestMetadata = {
      name: 'project_routing.delete',
      pathParts: {
        name: params.name
      },
      acceptedParams: [
        'name'
      ]
    }
    return await this.transport.request({ path, method, querystring, body, meta }, options)
  }

  /**
    * Get named project routing expressions. Get named project routing expressions.
    */
  async get (this: That, params: T.ProjectRoutingGetRequest, options?: TransportRequestOptionsWithOutMeta): Promise<T.ProjectRoutingGetResponse>
  async get (this: That, params: T.ProjectRoutingGetRequest, options?: TransportRequestOptionsWithMeta): Promise<TransportResult<T.ProjectRoutingGetResponse, unknown>>
  async get (this: That, params: T.ProjectRoutingGetRequest, options?: TransportRequestOptions): Promise<T.ProjectRoutingGetResponse>
  async get (this: That, params: T.ProjectRoutingGetRequest, options?: TransportRequestOptions): Promise<any> {
    const {
      path: acceptedPath
    } = this[kAcceptedParams]['project_routing.get']

    const userQuery = params?.querystring
    const querystring: Record<string, any> = userQuery != null ? { ...userQuery } : {}

    let body: Record<string, any> | string | undefined
    const userBody = params?.body
    if (userBody != null) {
      if (typeof userBody === 'string') {
        body = userBody
      } else {
        body = { ...userBody }
      }
    }

    for (const key in params) {
      if (acceptedPath.includes(key)) {
        continue
      } else if (key !== 'body' && key !== 'querystring') {
        // @ts-expect-error
        querystring[key] = params[key]
      }
    }

    const method = 'GET'
    const path = `/_project_routing/${encodeURIComponent(params.name.toString())}`
    const meta: TransportRequestMetadata = {
      name: 'project_routing.get',
      pathParts: {
        name: params.name
      },
      acceptedParams: [
        'name'
      ]
    }
    return await this.transport.request({ path, method, querystring, body, meta }, options)
  }

  /**
    * Get named project routing expressions. Get named project routing expressions.
    */
  async getMany (this: That, params?: T.ProjectRoutingGetManyRequest, options?: TransportRequestOptionsWithOutMeta): Promise<T.ProjectRoutingGetManyResponse>
  async getMany (this: That, params?: T.ProjectRoutingGetManyRequest, options?: TransportRequestOptionsWithMeta): Promise<TransportResult<T.ProjectRoutingGetManyResponse, unknown>>
  async getMany (this: That, params?: T.ProjectRoutingGetManyRequest, options?: TransportRequestOptions): Promise<T.ProjectRoutingGetManyResponse>
  async getMany (this: That, params?: T.ProjectRoutingGetManyRequest, options?: TransportRequestOptions): Promise<any> {
    const {
      path: acceptedPath
    } = this[kAcceptedParams]['project_routing.get_many']

    const userQuery = params?.querystring
    const querystring: Record<string, any> = userQuery != null ? { ...userQuery } : {}

    let body: Record<string, any> | string | undefined
    const userBody = params?.body
    if (userBody != null) {
      if (typeof userBody === 'string') {
        body = userBody
      } else {
        body = { ...userBody }
      }
    }

    params = params ?? {}
    for (const key in params) {
      if (acceptedPath.includes(key)) {
        continue
      } else if (key !== 'body' && key !== 'querystring') {
        // @ts-expect-error
        querystring[key] = params[key]
      }
    }

    const method = 'GET'
    const path = '/_project_routing'
    const meta: TransportRequestMetadata = {
      name: 'project_routing.get_many',
      acceptedParams: [
      ]
    }
    return await this.transport.request({ path, method, querystring, body, meta }, options)
  }
}
